export {}// type CartItem = {
//   id: string
//   quantity: number
// }

// type Props = {
//   id: string
//   items: CartItem[]
// }

// export const useHook = ({items}: Props) => {
//   const getItemQuantity = (id: string) => {
//     return items.find(item => item.id === id)?.quantity || 0;
//   }
//   return [getItemQuantity]
// }